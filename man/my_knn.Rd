% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/my_knn.R
\name{my_knn}
\alias{my_knn}
\title{K-Nearest Neighbors Classifier with Optional k Tuning}
\usage{
my_knn(train_data, test_data, k_values = c(3, 5, 7), tune = FALSE)
}
\arguments{
\item{train_data}{A data frame or matrix. Last column is the binary label (0 or 1).}

\item{test_data}{A data frame or matrix. Last column is the binary label (not used in prediction).}

\item{k_values}{A vector of candidate values for \code{k}. Default is \code{c(3, 5, 7)}.}

\item{tune}{Logical. If \code{TRUE}, performs internal evaluation to choose the best \code{k}.}
}
\value{
A list with:
\item{prediction}{Predicted class labels (0 or 1) for the test set.}
\item{probability}{Proportion of neighbors that voted for class 1 (for test set).}
\item{best_k}{The value of \code{k} used for final prediction.}
}
\description{
Predicts binary labels using the K-Nearest Neighbors algorithm.
Optionally selects the best \code{k} by internal validation from training data.
}
\examples{
# Create a simple dataset
train_data <- data.frame(x1 = c(1, 2, 3, 4), x2 = c(4, 3, 2, 1), y = c(0, 0, 1, 1))
test_data <- data.frame(x1 = c(2.5, 3.5), x2 = c(2.5, 1.5), y = c(0, 1))

# Run KNN
model <- my_knn(train_data, test_data, k = 3)
model$prediction
model$probability

}
\seealso{
\code{\link[assertthat]{assert_that}}
}
