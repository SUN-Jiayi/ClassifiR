% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dataPreprocess.R
\name{splitDataset_}
\alias{splitDataset_}
\title{Split a Data Frame into Training and Testing Sets}
\usage{
splitDataset_(data, test_ratio = 0.2, seed = 42)
}
\arguments{
\item{data}{A non-empty data frame.}

\item{test_ratio}{A numeric value between 0 and 1. Represents the proportion of test data. Default is 0.2.}

\item{seed}{An integer random seed to ensure reproducibility. Default is 42.}
}
\value{
A list with two data frames:
\describe{
  \item{train}{Training data frame.}
  \item{test}{Testing data frame.}
}
}
\description{
This function splits a data frame into a training set and a testing set
based on a user-defined ratio. The split is randomized but reproducible using a seed.
}
\examples{
df <- data.frame(x = 1:10, y = 11:20)
result <- splitDataset_(df, test_ratio = 0.3, seed = 123)
nrow(result$train) + nrow(result$test) == nrow(df)

}
